[PARSER]
    Name        amp_audit_json
    Format      json
    Time_Key    timestamp
    Time_Format %Y-%m-%dT%H:%M:%S.%L%z
    Time_Keep   On
    # Time_Key + Time_Keep On: use message timestamp as event time and keep
    # the field in the body so AMP signature verification still succeeds.
