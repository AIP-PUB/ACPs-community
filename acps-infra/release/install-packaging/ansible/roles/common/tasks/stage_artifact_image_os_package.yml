---
# stage_artifact_image_os_package.yml
# image 模式下 os_package 制品与其他 kind 共用同一 docker load 路径
# （image 清单不区分 kind，均为镜像归档）。
- name: 包含 image docker load 逻辑（os_package）
  ansible.builtin.include_tasks: stage_artifact_image.yml
