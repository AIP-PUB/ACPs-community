---
# stage_artifact_image_app_release.yml
# image 模式下 app_release 制品与其他 kind 共用同一 docker load 路径
# （image 清单不区分 kind，均为镜像归档）。
- name: 包含 image docker load 逻辑（app_release）
  ansible.builtin.include_tasks: stage_artifact_image.yml
