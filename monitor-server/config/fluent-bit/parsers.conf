[PARSER]
    Name        amp_audit_json
    Format      json
    Time_Key    timestamp
    Time_Format %Y-%m-%dT%H:%M:%S.%L%z
    Time_Keep   On
    # Time_Key + Time_Keep On：Fluent Bit 使用消息自带的 timestamp 作为事件时间，
    # 同时保留该字段在消息体中，保持原始 JSON 结构不变，避免签名验证失败。
