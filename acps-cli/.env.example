# acps-cli 运行时环境变量占位文件
# registry.auth.mode=local 时，可通过 REGISTRY_USER_USERNAME / REGISTRY_USER_PASSWORD
# 与 REGISTRY_ADMIN_USERNAME / REGISTRY_ADMIN_PASSWORD 提供本地账号登录凭据。
# registry.auth.mode=oidc、monitor.auth.mode=oidc 时，请改为配置
# ACPS_CLI_REGISTRY_OIDC_* / ACPS_CLI_MONITOR_OIDC_*，并先执行对应 auth login。
# auto-register 所需的默认显示名称和组织名请写在 acps-cli.toml 的 [registry] 中。
# 如后续需要用环境变量覆盖少量运行时选项，可在此文件中补充。
